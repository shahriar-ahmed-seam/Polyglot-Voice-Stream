# Audio I/O and orchestration (CPU)
sounddevice==0.4.6
numpy==1.26.4
python-dotenv==1.0.1

# Voice activity detection. Install torch/torchaudio from the CPU wheel index:
#   pip install torch==2.3.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cpu
# The default PyPI torchaudio is CUDA-enabled and fails on CPU-only hosts.
silero-vad==5.1
torch==2.3.1
torchaudio==2.3.1

# Inference APIs
groq==0.11.0
elevenlabs==1.8.0
httpx==0.27.2              # groq/elevenlabs pass proxies=, removed in httpx>=0.28

# Local CPU TTS (optional; enable with TTS_BACKEND=xtts)
# TTS==0.22.0

# Web backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
